import React from 'react';

// Composant vide - tout le contenu de navigation latérale a été supprimé
const SideNavigation = () => {
  // Nous ne retournons rien, ce composant est maintenant vide
  return null;
};

export default SideNavigation;